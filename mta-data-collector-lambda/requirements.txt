# Core dependencies for MTA delay tracking Lambda function
requests>=2.31.0
sqlalchemy>=2.0.0
psycopg2-binary==2.9.7
nyct-gtfs>=1.3.0

# AWS SDK (usually pre-installed in Lambda, but include for local development)
boto3>=1.26.0